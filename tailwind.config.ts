// Tailwind v4 theme values are defined in app/globals.css via @theme.
// This file exists only for IDE support and is not used by the build.
import type { Config } from "tailwindcss";

const config: Config = {
  content: ["./app/**/*.{ts,tsx}"],
};

export default config;
